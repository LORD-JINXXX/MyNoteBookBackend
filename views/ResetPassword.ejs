<!DOCTYPE html>

<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>MyNoteBook: Email Verification</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/ResetPassword.css">
    <script>

        window.onload = function () {
            const userId = "<%= userId %>";
            const token = "<%= token %>";
            const url = `http://localhost:3000/password-reset/${userId}/${token}`;

            document.getElementById("btnLogin").onclick = function () {
                window.location.href = `${url}`;
            };
        };

    </script>
</head>

<body>
    <div class="container">
        <div class="box">
            <h1>Link Verified Successfully!</h1>

            <p>You can reset your password by clicking below button.</p>

            <button class="btnLogin" id="btnLogin">Reset Password</button>
        </div>
    </div>
</body>

</html>